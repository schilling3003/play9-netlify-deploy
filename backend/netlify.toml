[build]
  command = "npm run build"
  functions = "functions"

[build.environment]
  NODE_VERSION = "18"
  SQLITE_DB_PATH = "./golf.db"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server"
  status = 200
