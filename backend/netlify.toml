[build]
  command = "npm install && npm run build"
  functions = "functions"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "10.7.0"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server"
  status = 200
